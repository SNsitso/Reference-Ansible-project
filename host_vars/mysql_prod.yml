---
# Variables spécifiques au serveur MySQL Production
# Host: mysql_prod (192.168.56.11)

# Identité de l'hôte
hostname: mysql-server-prod
environment_type: production

# Secrets : les valeurs viennent du vault de l'inventaire
# (inventories/production/group_vars/all/vault.yml)
mysql_root_password: "{{ vault_mysql_root_password }}"
mysql_app_password: "{{ vault_mysql_app_password }}"

# Base de données applicative
mysql_database: prestashop_db
mysql_app_user: prestashop_user
mysql_app_user_host: '%'  # accessible depuis les autres machines du réseau privé

# Optimisations Production
mysql_max_connections: 200
mysql_innodb_buffer_pool_size: 1G
mysql_slow_query_log: true
mysql_slow_query_time: 2

# Backup Production
mysql_backup_enabled: true
mysql_backup_hour: 2
mysql_backup_minute: 0
